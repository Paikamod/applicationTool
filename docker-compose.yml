services:
  applicationtool:
    build: .
    container_name: applicationtool
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
